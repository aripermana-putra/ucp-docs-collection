# 2026-06-01

## What I accomplished

Wrapped up MCUCP-191 documentation and ran live Horizon Core Data API tests.

- Wrote the MCUCP-191 POC report following the standard template and placed it in `pocs/rbac/poc-report.md`
- Cleaned up stale Horizon API statements across the RBAC docs — the "planned optimisation" framing for JWT sync was wrong (already implemented), the `fetchOCMemberTenants` reference was stale, and the "eliminate entirely" claim about the `/members` call was misleading
- Updated `horizon-core-data-api.md` and added references (OpenAPI spec, Core Data overview, API registry, releases changelog)
- Wired up 5 test proxy endpoints in the api-server for live Horizon testing (Tests 1–5), deployed, and ran all tests with actual request/response captured in docs
- Key findings from testing: `subscriptions[].default_role` is not reliably populated — Test 4 (`GET /v0/members/{rns}/tenants/{rns}/services/{rns}/access/roles?verify`) is the only reliable real-time service role check for Option 2; user token is sufficient for all endpoints
- Documented the two Option 2 implementation paths (live Horizon check vs cached `oc_roles`)
- Cleaned up the POC report — removed implementation-specific noise (goroutine race, FK constraint detail, tenants table fallback) that has no bearing on MVP decisions

## What I thought

The Horizon API testing surfaced something important: `default_role` in the subscriptions response is basically useless for role enforcement — only `billing` had it. Good that we tested before assuming it would work for Option 2.

The POC report cleanup was overdue. A POC report should help the next person decide whether to build the thing in production — it shouldn't be a diary of implementation gotchas.

## Next actions

- Get a non-admin Horizon test account to verify JWT `groups` format for Tenant Members (Open Question 1) — this would simplify the sync
- PM alignment needed on Option 2 and non-user member type handling before MVP scope is locked
- Schema decision on single vs two-table for OC member identity before any MVP migration is written
